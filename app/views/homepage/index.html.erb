<h3>美股財報發散度調查</h3>

<hr>

<h4><%= link_to "Google Finance 標準財報欄位列表", standard_items_path %></h4>
<h4><%= link_to "個股列表", stocks_path %></h4>
<h4><%= link_to "財報列表", statements_path %></h4>

<hr>

<h3>資料量統計：</h3>

<table class="table table-striped table-condensed">
  <thead>
    <th style="width: 25%">項目</th>
    <th>總數</th>
  </thead>
  <tbody>
    <tr><td>股票</td><td><%= Stock.all.size %></td></tr>
    <% st_size = Statement.all.size %>
    <tr><td>財報</td><td><%= st_size %>（約 <%= st_size * 3.3 / 1000 %> GB）</td></tr>
    <tr><td>XBRL名稱</td><td><%= Item.all.size %></td></tr>
    <tr><td>標準財報欄位</td><td><%= StandardItem.all.size %></td></tr>
    <tr><td>XBRL名稱 - 股票 配對</td><td><%= ItemStockPair.all.size %></td></tr>
    <tr><td>XBRL名稱 - 財報 配對</td><td><%= ItemStatementPair.all.size %></td></tr>
    <tr><td>XBRL名稱 - 標準財報欄位 配對</td><td><%= ItemStandardItemPair.all.size %></td></tr>
  </tbody>
</table>

